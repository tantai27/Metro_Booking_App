<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.metro_booking_app">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MetroBookingApp.Light"
        tools:targetApi="31">

        <!-- Màn hình khởi động là Register -->
        <activity
            android:name=".RegisterActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Các Activity còn lại -->
        <activity android:name=".LoginActivity" android:exported="true" />
        <activity android:name=".MainActivity" android:exported="true" />
        <activity android:name=".SelectTicketTypeActivity" android:exported="true" />
        <activity android:name=".SelectRouteActivity" android:exported="true" />
        <activity android:name=".ConfirmTicketActivity" android:exported="true" />
        <activity android:name=".PaymentActivity" android:exported="true" />
        <activity android:name=".ResetPasswordActivity" android:exported="true" />
        <activity android:name=".AccountActivity" />
        <activity android:name=".PrintDatabaseActivity" />
        <activity android:name=".ForgotPasswordActivity" />
        <activity android:name=".EditProfileActivity" />

    </application>
</manifest>

